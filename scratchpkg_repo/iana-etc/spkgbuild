# description	: Provides data for network services and protocols

name=iana-etc
version=$iana_etc_version
release=1
source="services protocols"

build() {
	install -d -m 755 $PKG/etc
	install -m 644 services $PKG/etc
	install -m 644 protocols $PKG/etc
}
